# name: Deploy Frontend to Railway

# on:
#   push:
#     branches: [main]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Install Railway CLI
#         run: npm i -g @railway/cli

#       - name: Deploy to Production
#         run: railway up --service ApointmantAppByTosho --environment production
#         env:
#           RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
name: Build Check
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
